const putTours = async (id) =>{

  try {
  
      if (id) {
        
        }
        throw Error ('invalid data')
  
    } catch (error) {
  
      throw error('insufficient data')
      
    }
    
  }

  module.exports = {putTours}