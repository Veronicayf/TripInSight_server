const postAdmin = async (id) =>{

    try {
    
        if (id) {
          
          }
          throw Error ('invalid data')
    
      } catch (error) {
    
        throw error('insufficient data')
        
      }
      
    }

    module.exports = {postAdmin}